import { Builder, withChildren } from '@builder.io/react'
import {{ pascalCase name }} from '.'

export default function register{{ pascalCase name }}(): void {
  Builder.registerComponent(withChildren({{ pascalCase name }}), {
    name: '{{ sentenceCase name }}',
    defaultChildren: [
      {
        '@type': '@builder.io/sdk:Element',
        component: {
          name: 'Text',
          options: { text: 'Put content here' },
        },
      },
    ],
    inputs: [
      {
        name: 'className',
        type: 'string',
        defaultValue: '',
      },
    ],
  })
}
