---
labels: ['customer', 'data']
description: 'Transform server response to standard format'
---

```js
normalizeCustomer(serverResponseWithCutomerData)
.then(normalizedCustomer => console.log(normalizedCustomer.fullName))
```