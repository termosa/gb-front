import YotpoStarRating from '.'

export const meta = {
  type: 'component',
  name: 'YotpoStarRating',
  description: 'Yotpo widget showing product stars',
  tags: ['component', 'yotpo'],
  preview: <YotpoStarRating productId={68415324184} />,
  lists: {
    attributes: [
      { name: 'className', type: 'classnames.Argument', description: 'One or multiple class names that will be added to the main container' },
      { name: 'style', type: 'React.CSSProperties', description: 'Inline styles for the main container' },
      { name: 'productId', type: 'number', description: 'Will override the corresponding value from ProductContext' },
    ],
  },
  scope: { YotpoStarRating },
}

## Usage

With context

```jsx
<ProductContext.Provider value={{ product_id: 68415324184 }}>
  <YotpoStarRating />
</ProductContext.Provider>
```

Setting product details within attributes

```jsx
<YotpoStarRating productId={68415324184} />
```

## Playground

```jsx live
<YotpoStarRating productId={68415324184} />
```
