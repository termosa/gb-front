export const meta = {
  type: 'function',
  name: 'normalizeCustomerOrdersDetails',
  description: 'Normalize customer orders details data',
  tags: ['function', 'customer'],
  lists: {
    arguments: [
      {
        name: 'serverCustomerOrdersDetails',
        type: 'ServerCustomerOrdersDetails',
        description: 'Server cusotmer orders details',
        required: true,
      },
    ],
    returns: [
      { name: 'customerOrdersDetails', type: 'object', description: 'Normalized customer orders details data' },
      { name: 'customerOrdersDetails.totalPoints', type: 'number', description: 'Total bonus points' },
      { name: 'customerOrdersDetails.isICMember', type: 'boolean', description: 'Is the customer a member of the Inner Circle program' },
      { name: 'customerOrdersDetails.level', type: '{ NOIR = 0, GOLD = 1, PLATINUM = 2 }', description: 'Level of the customer bonus program' },
      { name: 'customerOrdersDetails.isICMembershipActive', type: 'boolean', description: 'Is the customer an active member of the Inner Circle program' },
    ],
  },
}

## Usage

```js
import normalizeCustomerOrdersDetails from './normalize-customer-orders-details'
import api from '../../../src/modules/api'

api({
  path: `/customers/customer_data/`,
  query: { customer_email: email },
}).then(normalizeCustomerOrdersDetails)
```
