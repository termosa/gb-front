import loadYotpoScript from '.'

export const meta = {
  type: 'hook',
  name: 'loadYotpoScript',
  description: 'Initializes Yotpo script',
  tags: ['hook', 'yotpo'],
}

## Usage

```jsx
const ProductRating = ({ productId }) => {
  loadYotpoScript()
  return <div class="yotpo bottomLine" />
}
```
