export const meta = {
  type: 'function',
  name: 'loadCustomerOrdersDetails',
  description: 'Loads customer orders details and returns normalized data',
  tags: ['function', 'customer'],
  lists: {
    arguments: [{ name: 'email', type: 'string', description: 'Customer email', required: true }],
    returns: [
      {
        name: 'customerOrdersDetails',
        type: 'normalize-customer-orders-details.CustomerOrdersDetails',
        description: 'Normalized customer orders details',
      },
    ],
  },
}

## Usage

```js
import loadCustomerOrdersDetails from './load-customer-orders-details'

loadCustomerOrdersDetails('customerEmail@email.com').then((r) =>
  console.log('Total customer points is: ', r.totalPoints)
)
```
