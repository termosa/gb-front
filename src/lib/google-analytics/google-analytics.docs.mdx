export const meta = {
  type: 'function',
  name: 'googleAnalytics',
  description: 'Submit analytics data to Google',
  tags: ['function', 'analytics'],
  lists: {
    arguments: [
      { name: 'command', type: 'string', required: true },
      { name: '...properties', type: 'Array<unknown>', required: true },
    ],
    returns: [
      { name: 'promise', type: 'Promise<void>' },
    ],
  },
}

## Usage

```js
import googleAnalytics from './google-analytics'

googleAnalytics('pageview')
```
