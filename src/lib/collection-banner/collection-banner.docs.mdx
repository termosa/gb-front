import CollectionBanner from '.'

export const meta = {
  type: 'component',
  name: 'CollectionBanner',
  description: 'Banner for collection page',
  tags: ['component'],
  preview: <CollectionBanner>Component content here</CollectionBanner>,
  lists: {
    attributes: [
      { name: 'children', required: true, type: 'React.ReactNode', description: 'Content that will be rendered inside of the block' },
      { name: 'className', type: 'classnames.Argument', description: 'One or multiple class names that will be added to the main container' },
      { name: 'style', type: 'React.CSSProperties', description: 'Inline styles for the main container' },
    ],
  },
  scope: { CollectionBanner },
}

## Usage

```jsx
<CollectionBanner className="class" style={{ color: 'red' }}>Put content here</CollectionBanner>
```

## Playground

```jsx live
<CollectionBanner style={{ color: 'red' }}>Content text</CollectionBanner>
```
