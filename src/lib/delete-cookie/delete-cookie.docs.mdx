export const meta = {
  type: 'function',
  name: 'deleteCookie',
  description: 'Delete cookies',
  tags: ['function'],
  lists: {
    arguments: [
      { name: 'name', type: 'string', description: 'Name cookie' },
    ],
  },
}

## Usage

```js
import deleteCookie from './delete-cookies'

deleteCookie('promo_variant')
```
