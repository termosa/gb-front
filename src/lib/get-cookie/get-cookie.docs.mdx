export const meta = {
  type: 'function',
  name: 'getCookie',
  description: 'Get cookies',
  tags: ['function'],
  lists: {
    arguments: [
      { name: 'name', type: 'string', description: 'Name cookie' },
    ],
    returns: [
      { name: 'value', type: 'string', description: 'Value cookie' },
    ],
  },
}

## Usage

```js
import getCookie from './get-cookies'

getCookie('promo_variant')
```
