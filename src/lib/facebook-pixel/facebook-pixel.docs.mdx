export const meta = {
  type: 'function',
  name: 'facebook-pixel',
  description: 'Submits analytics data to Facebook',
  tags: ['function', 'analytics'],
  lists: {
    arguments: [
      { name: 'eventName', type: 'string', required: true },
    ],
    returns: [
      { name: 'promise', type: 'Promise<void>' },
    ],
  },
}

## Usage

```js
import facebookPixel from './facebook-pixel'

facebookPixel('track', 'PageView')
```
