import SiteFooter from '.'

export const meta = {
  type: 'component',
  name: 'SiteFooter',
  description: 'Site bottom with with links to main pages and social networks',
  tags: ['component', 'layout'],
  preview: <SiteFooter />,
  lists: {
    attributes: [
      { name: 'className', type: 'classnames.Argument', description: 'One or multiple class names that will be added to the main container' },
      { name: 'style', type: 'React.CSSProperties', description: 'Inline styles for the main container' },
    ],
  },
}

## Usage

```jsx
<SiteFooter className="class" style={{ borderTop: '2px solid gray' }} />
```
