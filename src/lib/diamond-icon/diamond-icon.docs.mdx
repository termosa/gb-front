import DiamondIcon from '.'
import { CustomerLevel } from '../normalize-customer-orders-details'

export const meta = {
  type: 'component',
  name: 'DiamondIcon',
  description: 'Pass customer level to set diamond style',
  tags: ['component', 'icon'],
  preview: <DiamondIcon customerLevel={CustomerLevel.NOIR} />,
  lists: {
    attributes: [
      {
        name: 'customerLevel',
        required: true,
        type: '{ NOIR = 0 | GOLD = 1 | PLATINUM = 2 }',
        description: 'A customer level depending on which will be displayed Noir(0), Gold(1) or Platinum(2) diamond',
      },
    ],
  },
  scope: { DiamondIcon },
}

## Playground

```jsx live render
render(() => {
  const CustomerLevel = {
    NOIR: 0,
    GOLD: 1,
    PLATINUM: 2,
  }

  return <DiamondIcon customerLevel={CustomerLevel.GOLD} />
})
```
