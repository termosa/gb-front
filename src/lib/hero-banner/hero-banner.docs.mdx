import HeroBanner from '.'

export const meta = {
  type: 'component',
  name: 'HeroBanner',
  description: 'Styled block with custom content',
  tags: ['component'],
  preview: <HeroBanner>Component content here</HeroBanner>,
  lists: {
    attributes: [
      { name: 'children', required: true, type: 'React.ReactNode', description: 'Content that will be rendered inside of the block' },
      { name: 'className', type: 'classnames.Argument', description: 'One or multiple class names that will be added to the main container' },
      { name: 'style', type: 'React.CSSProperties', description: 'Inline styles for the main container' },
    ],
  },
  scope: { HeroBanner },
}

## Usage

```jsx
<HeroBanner className="class" style={{ color: 'red' }}>Put content here</HeroBanner>
```

## Playground

```jsx live
<HeroBanner style={{ color: 'red' }}>Content text</HeroBanner>
```
