export const meta = {
  type: 'function',
  name: 'navigate',
  description: 'Navigates to the given page url',
  tags: ['function'],
  lists: {
    arguments: [
      {
        name: 'path',
        type: 'string',
        description: 'Url of page you want to navigate (e.g. "/cart", "/products/1", etc.)',
        required: true,
      },
    ],
  },
}

## Usage

```js
import navigate from './navigate'

navigate('/cart')
```
