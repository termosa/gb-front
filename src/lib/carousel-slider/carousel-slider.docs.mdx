import CarouselSlider from '.'

export const meta = {
  type: 'component',
  name: 'CarouselSlider',
  description: 'Styled block with custom content',
  tags: ['component'],
  lists: {
    attributes: [
      { name: 'partiallyVisible', type: 'boolean', description: 'Should gallery have one slide that overflows' },
      { name: 'scrollbarPresent', required: true, type: 'boolean', description: 'Condition for horizontal scrollbar' },
      { name: 'dotsPresent', type: 'boolean', description: 'Condition for dots' },
      { name: 'arrows', required: true, type: 'boolean | null', description: 'Condition for arrows' },
      { name: 'itemClass', type: 'string', description: 'Class for carousel item' },
      { name: 'children', required: true, type: 'JSX.Element[]', description: 'Content that will be rendered inside of the block' },
    ],
  },
  scope: { CarouselSlider },
}

## Playground

```jsx live render
const CarouselPreview = () => {
  const [isScrollbarPresent, setScrollbarPresent] = useState()
  const [isDotsPresent, setDotsPresent] = useState()
  const sliderSettings = {
    desktop: {
       breakpoint: { max: 3000, min: 768 },
       items: isPartiallyVisible ? 3 : 1,
    },
    tablet: {
       breakpoint: { max: 768, min: 464 },
       items: isPartiallyVisible ? 2.5 : 1,
    },
    mobile: {
       breakpoint: { max: 464, min: 0 },
       items: isPartiallyVisible ? 1.5 : 1,
    },
  }
  return (
  <CarouselSlider
    responsive={sliderSettings}
    arrows={true}
    scrollbarPresent={isScrollbarPresent}
    dotsPresent={isDotsPresent}
  >
     {[...new Array(5).fill()].map((_, i) => {
        return <img src={"https://source.unsplash.com/random/800x800/?img=" + i} alt="" style={{width: "300px"}}/>
     })}
  </CarouselSlider>
  )
}

render(<CarouselPreview />)
```
