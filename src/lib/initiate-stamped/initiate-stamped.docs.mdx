export const meta = {
  type: 'function',
  name: 'initiateStamped',
  description: 'Loads Stamped script and executes init()',
  tags: ['function'],
  lists: {
    returns: [
      { name: 'promise', type: 'Promise<void>' },
    ],
  },
}

## Usage

```js
import initiateStamped from './initiate-stamped'

initiateStamped()
```
