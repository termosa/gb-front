import React from 'react'
import PresentationCanvas from '../../helpers/presentation-canvas'
import { FollowUs } from './follow-us'

export const BasicFollowUs = (): React.ReactElement => (
  <PresentationCanvas>
    <FollowUs />
  </PresentationCanvas>
)
