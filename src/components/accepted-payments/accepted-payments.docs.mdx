# An AcceptedPayments component

import { AcceptedPayments } from './accepted-payments'
import PresentationCanvas from '../../helpers/presentation-canvas'

### Sample

<PresentationCanvas dark>
  <AcceptedPayments />
</PresentationCanvas>

export const tags = ['component']
